---
phase: 03-integration-and-connection-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/v2x-discharge-guide.md
autonomous: false
requirements:
  - GUIDE-01

must_haves:
  truths:
    - 'A developer can find the guide at docs/v2x-discharge-guide.md'
    - 'The guide explains how to start CitrineOS via Docker Compose'
    - 'The guide shows the WebSocket URL (ws://localhost:8083/{stationId}) and subprotocol (ocpp2.1)'
    - 'The guide contains copy-paste JSON examples for all 9 messages in the V2X discharge flow'
    - 'Each message step documents both the station request AND the expected CSMS response'
    - 'The guide honestly documents which messages return NotSupported (Authorize, TransactionEvent, MeterValues)'
    - 'The guide includes wscat commands for each message step'
    - 'The guide has a troubleshooting section with 3-5 common issues'
  artifacts:
    - path: 'docs/v2x-discharge-guide.md'
      provides: 'Complete V2X discharge connection guide'
      min_lines: 300
      contains: 'ws://localhost:8083'
  key_links:
    - from: 'docs/v2x-discharge-guide.md'
      to: 'Server/docker-compose.yml'
      via: 'Docker setup reference'
      pattern: 'docker compose up'
    - from: 'docs/v2x-discharge-guide.md'
      to: 'Server/src/config/envs/docker.ts'
      via: 'Port 8083 OCPP 2.1 config reference'
      pattern: '8083'
---

<objective>
Create a comprehensive V2X discharge connection guide that documents how to connect an OCPP 2.1 charging station client to CitrineOS via Docker and execute a complete V2X discharge session.

Purpose: This is the capstone deliverable for the project -- a developer-facing guide that validates all Phase 1 and Phase 2 work by demonstrating the end-to-end V2X discharge flow with concrete, copy-paste JSON/wscat examples. It honestly documents both working handlers and known gaps.

Output: `docs/v2x-discharge-guide.md` -- standalone markdown guide with Docker setup, WebSocket connection instructions, 9-message V2X flow with JSON examples, and troubleshooting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-integration-and-connection-guide/03-CONTEXT.md
@.planning/phases/03-integration-and-connection-guide/03-RESEARCH.md
@.planning/phases/01-foundation-fixes/01-01-SUMMARY.md
@.planning/phases/02-v2x-module-handlers/02-01-SUMMARY.md
@.planning/phases/02-v2x-module-handlers/02-02-SUMMARY.md

@Server/docker-compose.yml
@Server/src/config/envs/docker.ts
@Server/everest/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create V2X discharge connection guide</name>
  <files>docs/v2x-discharge-guide.md</files>
  <read_first>
    - Server/docker-compose.yml (Docker services, ports, healthcheck, startup order)
    - Server/src/config/envs/docker.ts (OCPP 2.1 WebSocket config: port 8083, protocol, allowUnknownChargingStations)
    - Server/everest/README.md (format reference for connection guide style)
    - 00_Base/src/ocpp/rpc/message.ts (MessageTypeId enum: Call=2, CallResult=3, CallError=4; OCPP_2_VER_LIST definition)
    - 00_Base/src/ocpp/model/2.1/schemas/BootNotificationRequest.json (BootNotification schema for accurate JSON)
    - 00_Base/src/ocpp/model/2.1/schemas/NotifyEVChargingNeedsRequest.json (V2X charging needs schema)
    - 00_Base/src/ocpp/model/2.1/schemas/TransactionEventRequest.json (TransactionEvent schema with Discharging)
    - 00_Base/src/ocpp/model/2.1/schemas/StatusNotificationRequest.json (StatusNotification schema)
    - 00_Base/src/ocpp/model/2.1/schemas/AuthorizeRequest.json (Authorize schema)
    - 00_Base/src/ocpp/model/2.1/schemas/MeterValuesRequest.json (MeterValues schema with V2X measurands)
    - 03_Modules/SmartCharging/src/module/module.ts (NotifyEVChargingNeeds handler -- confirm BPT acceptance logic)
    - 03_Modules/EVDriver/src/module/module.ts (NotifyAllowedEnergyTransfer stub handler location)
    - 00_Base/src/interfaces/modules/AbstractModule.ts (NotSupported error generation at lines 179-182)
  </read_first>
  <action>
Create the directory `docs/` and write `docs/v2x-discharge-guide.md` with the following structure and content:

**File header:** SPDX license header matching project convention (Apache-2.0).

**Section 1: Title and Overview**

- Title: "V2X Discharge Connection Guide -- OCPP 2.1"
- Brief intro: This guide walks through connecting an OCPP 2.1 charging station client to CitrineOS and executing a V2X (Vehicle-to-Grid) DC discharge session.
- Per D-06: Commercial V2G DC scenario with ~50kW discharge power, DC_BPT energy transfer mode.
- Per D-07: Station ID "CS001", works with default `allowUnknownChargingStations: true`.

**Section 2: What Works Today (Handler Support Matrix)**

- Table documenting the current handler version support per research:

| Message                     | OCPP 2.1 Handler      | Expected Response           |
| --------------------------- | --------------------- | --------------------------- |
| BootNotification            | Yes (OCPP_2_VER_LIST) | Accepted                    |
| StatusNotification          | Yes (OCPP_2_VER_LIST) | Empty response (OK)         |
| Authorize                   | No (2.0.1 only)       | NotSupported CallError      |
| TransactionEvent            | No (2.0.1 only)       | NotSupported CallError      |
| NotifyEVChargingNeeds       | Yes (OCPP_2_VER_LIST) | Accepted (with BPT profile) |
| NotifyAllowedEnergyTransfer | N/A (CSMS-to-station) | Station receives, not sends |
| MeterValues                 | No (2.0.1 only)       | NotSupported CallError      |

- Note: Authorize, TransactionEvent, and MeterValues handlers are registered only for OCPP 2.0.1 (via `@AsHandler([OCPPVersion.OCPP2_0_1], ...)`). When a station connects on the `ocpp2.1` subprotocol and sends these actions, the server returns a `NotSupported` CallError because `AbstractModule.handle()` at line 179-182 finds no matching handler.

**Section 3: Prerequisites**

- Docker and Docker Compose installed
- wscat installed: `npm install -g wscat` (or use `npx wscat`)
- CitrineOS repository cloned and on `feature/ocpp-2.1` branch
- No station pre-registration needed (allowUnknownChargingStations: true)

**Section 4: Starting CitrineOS (per D-02, Docker-only)**

- Commands:
  ```
  cd Server
  docker compose up -d
  ```
- Wait for healthy status:
  ```
  docker compose ps
  ```
  - Wait until `citrine` shows `healthy` (60s start period, healthcheck on port 8080)
- Service overview table (from docker-compose.yml):
  | Service | Port | Purpose |
  |---------|------|---------|
  | citrine | 8080 | HTTP API |
  | citrine | 8083 | OCPP 2.1 WebSocket (security profile 0) |
  | ocpp-db | 5432 | PostgreSQL |
  | amqp-broker | 5672/15672 | RabbitMQ |
  | graphql-engine | 8090 | Hasura GraphQL |

**Section 5: Connecting via WebSocket (per D-08)**

- wscat command: `wscat -c ws://localhost:8083/CS001 -s ocpp2.1`
- Explain: Port 8083 is the OCPP 2.1 WebSocket server. Station ID "CS001" is extracted from the URL path. The `-s ocpp2.1` flag sets the `Sec-WebSocket-Protocol` header.
- Expected: Connected prompt appears. If connection drops immediately, check subprotocol (see Troubleshooting).

**Section 6: V2X Discharge Message Sequence (per D-03, D-04, D-05)**

For each of the 9 messages below, include:

1. Step number and message name with direction arrow
2. Brief description of what this step does
3. Full JSON of the OCPP Call (station -> CSMS)
4. Full JSON of the expected CSMS response (CallResult or CallError)
5. The wscat send command (copy-paste ready, format: `> [2,"id","Action",{payload}]`)
6. Notes on what to expect

**Step 1: BootNotification (Station -> CSMS) -- WORKS**

Station request:

```json
[
  2,
  "boot-1",
  "BootNotification",
  {
    "reason": "PowerUp",
    "chargingStation": {
      "model": "V2X-DC-50",
      "vendorName": "TestVendor",
      "serialNumber": "SN-V2X-001"
    }
  }
]
```

Expected CSMS response (CallResult):

```json
[3, "boot-1", { "currentTime": "2026-03-26T12:00:00.000Z", "interval": 60, "status": "Accepted" }]
```

wscat: type/paste the Call JSON after the `>` prompt.
Note: `currentTime` will be the actual server time. `interval` is the heartbeat interval in seconds. Status will be `Accepted` because Docker config sets `unknownChargerStatus: "Accepted"` and `autoAccept: true`.

**Step 2: StatusNotification (Station -> CSMS) -- WORKS**

Station request:

```json
[
  2,
  "status-1",
  "StatusNotification",
  {
    "timestamp": "2026-03-26T12:00:05.000Z",
    "connectorStatus": "Available",
    "evseId": 1,
    "connectorId": 1
  }
]
```

Expected CSMS response (CallResult -- empty per OCPP 2.1 spec):

```json
[3, "status-1", {}]
```

Note: StatusNotification has an empty response body in OCPP 2.1.

**Step 3: Authorize (Station -> CSMS) -- NotSupported**

Station request:

```json
[2, "auth-1", "Authorize", { "idToken": { "idToken": "RFID-001", "type": "ISO14443" } }]
```

Expected CSMS response (CallError):

```json
[4, "auth-1", "NotSupported", "No handler found for action: Authorize at module evdriver", {}]
```

Note: The Authorize handler is only registered for OCPP 2.0.1. This is a known gap -- the handler at `EVDriver/module.ts` uses `@AsHandler([OCPPVersion.OCPP2_0_1], ...)`. For testing V2X-specific functionality, this step can be skipped. Some stations support `NoAuthorization` mode where authorization is embedded in TransactionEvent.

**Step 4: TransactionEvent Started (Station -> CSMS) -- NotSupported**

Station request (per D-06 using DC_BPT scenario):

```json
[
  2,
  "tx-started-1",
  "TransactionEvent",
  {
    "eventType": "Started",
    "timestamp": "2026-03-26T12:01:00.000Z",
    "triggerReason": "Authorized",
    "seqNo": 0,
    "transactionInfo": { "transactionId": "V2X-TX-001", "chargingState": "EVConnected" },
    "evse": { "id": 1, "connectorId": 1 },
    "idToken": { "idToken": "RFID-001", "type": "ISO14443" }
  }
]
```

Expected CSMS response (CallError):

```json
[
  4,
  "tx-started-1",
  "NotSupported",
  "No handler found for action: TransactionEvent at module transactions",
  {}
]
```

Note: TransactionEvent handler is only registered for OCPP 2.0.1. This means no transaction record is created in the database. This impacts Step 5 (NotifyEVChargingNeeds) because that handler checks for an active transaction.

**Step 5: NotifyEVChargingNeeds (Station -> CSMS) -- WORKS (but depends on Step 4)**

Station request (per D-06, DC_BPT with ~50kW):

```json
[
  2,
  "needs-1",
  "NotifyEVChargingNeeds",
  {
    "evseId": 1,
    "chargingNeeds": {
      "requestedEnergyTransfer": "DC_BPT",
      "v2xChargingParameters": {
        "minChargePower": 1000,
        "maxChargePower": 50000,
        "minDischargePower": 1000,
        "maxDischargePower": 50000,
        "minChargeCurrent": 2,
        "maxChargeCurrent": 125,
        "minDischargeCurrent": 2,
        "maxDischargeCurrent": 125,
        "minVoltage": 200,
        "maxVoltage": 500,
        "evTargetEnergyRequest": -10000,
        "targetSoC": 30
      }
    }
  }
]
```

Expected CSMS response when transaction exists (CallResult):

```json
[3, "needs-1", { "status": "Accepted" }]
```

Expected CSMS response when NO transaction exists (CallResult):

```json
[3, "needs-1", { "status": "Rejected" }]
```

Note: The handler is registered for OCPP 2.1 and accepts DC_BPT mode (Phase 2 work). However, it queries for an active transaction on EVSE 1. Since TransactionEvent in Step 4 returned NotSupported, no transaction was created, so this will likely return `Rejected`. This confirms the V2X handler code path works correctly (it validates BPT params, checks for transaction) even though the end-to-end flow is blocked by the TransactionEvent gap. If a transaction were created (e.g., via REST API or using OCPP 2.0.1 subprotocol), this would return `Accepted` and trigger a SetChargingProfile with dischargeLimit set to negative maxDischargePower (-50000).

**Step 6: NotifyAllowedEnergyTransfer (CSMS -> Station) -- Cannot be demonstrated via wscat**

This is a CSMS-to-station message. The CSMS sends it TO the station to inform which energy transfer modes are allowed. There is currently no automated trigger or REST API endpoint to initiate this message.

Document the message format the station client should be prepared to RECEIVE:

```json
[2, "allowed-energy-1", "NotifyAllowedEnergyTransfer", { "allowedEnergyTransfer": ["DC_BPT"] }]
```

Expected station response (station sends back):

```json
[3, "allowed-energy-1", { "status": "Accepted" }]
```

Note: The Phase 2 stub handler at EVDriver/module.ts processes the station's response and logs the status. In a full implementation, the CSMS would send this after processing charging needs. For now, this step is informational only.

**Step 7: MeterValues with discharge data (Station -> CSMS) -- NotSupported**

Station request (per D-06, showing discharge/export measurands):

```json
[
  2,
  "meter-1",
  "MeterValues",
  {
    "evseId": 1,
    "meterValue": [
      {
        "timestamp": "2026-03-26T12:10:00.000Z",
        "sampledValue": [
          {
            "measurand": "Energy.Active.Export.Register",
            "value": 5000,
            "unitOfMeasure": { "unit": "Wh" }
          },
          { "measurand": "Power.Active.Export", "value": 48500, "unitOfMeasure": { "unit": "W" } },
          { "measurand": "Display.PresentSOC", "value": 65, "unitOfMeasure": { "unit": "Percent" } }
        ]
      }
    ]
  }
]
```

Expected CSMS response (CallError):

```json
[
  4,
  "meter-1",
  "NotSupported",
  "No handler found for action: MeterValues at module transactions",
  {}
]
```

Note: MeterValues handler is only registered for OCPP 2.0.1. Phase 1 added export energy measurand support to MeterValueUtils (getExportRegisterValuesMap, bidirectional net energy calculation), but the handler itself has not been extended to OCPP 2.1 yet.

**Step 8: TransactionEvent Updated (Station -> CSMS) -- NotSupported**

Station request (showing Discharging state, per Phase 1 enum addition):

```json
[
  2,
  "tx-updated-1",
  "TransactionEvent",
  {
    "eventType": "Updated",
    "timestamp": "2026-03-26T12:15:00.000Z",
    "triggerReason": "ChargingStateChanged",
    "seqNo": 1,
    "transactionInfo": { "transactionId": "V2X-TX-001", "chargingState": "Discharging" },
    "evse": { "id": 1, "connectorId": 1 },
    "meterValue": [
      {
        "timestamp": "2026-03-26T12:15:00.000Z",
        "sampledValue": [
          {
            "measurand": "Energy.Active.Export.Register",
            "value": 8000,
            "unitOfMeasure": { "unit": "Wh" }
          }
        ]
      }
    ]
  }
]
```

Expected CSMS response (CallError):

```json
[
  4,
  "tx-updated-1",
  "NotSupported",
  "No handler found for action: TransactionEvent at module transactions",
  {}
]
```

Note: The `Discharging` chargingState value was added to the DTO enums in Phase 1 (VALID-02). The JSON schema validates correctly. However, the handler itself only supports OCPP 2.0.1.

**Step 9: TransactionEvent Ended (Station -> CSMS) -- NotSupported**

Station request:

```json
[
  2,
  "tx-ended-1",
  "TransactionEvent",
  {
    "eventType": "Ended",
    "timestamp": "2026-03-26T12:30:00.000Z",
    "triggerReason": "EVDeparted",
    "seqNo": 2,
    "transactionInfo": { "transactionId": "V2X-TX-001", "chargingState": "Idle" },
    "evse": { "id": 1, "connectorId": 1 },
    "meterValue": [
      {
        "timestamp": "2026-03-26T12:30:00.000Z",
        "sampledValue": [
          {
            "measurand": "Energy.Active.Export.Register",
            "value": 10000,
            "unitOfMeasure": { "unit": "Wh" }
          }
        ]
      }
    ]
  }
]
```

Expected CSMS response (CallError):

```json
[
  4,
  "tx-ended-1",
  "NotSupported",
  "No handler found for action: TransactionEvent at module transactions",
  {}
]
```

**Section 7: Summary of V2X Capabilities Implemented**

Bullet list summarizing what Phases 1-2 delivered:

- OCPPValidator correctly validates OCPP 2.1 requests against request schemas (not response)
- ChargingState "Discharging" accepted in DTO validation and JSON schemas
- V2X measurands (Energy.Active.Export._, Display._, Setpoint.\*) pass validation
- MeterValueUtils computes bidirectional net energy (import - export)
- NotifyEVChargingNeeds accepts DC_BPT mode with V2X charging parameters
- Profile calculator generates discharge-aware ChargingProfiles with dischargeLimit
- ChargingProfile mapper preserves OCPP 2.1 fields through persistence
- NotifyAllowedEnergyTransfer routed to EVDriver in all config environments

Known gaps for future work:

- Authorize, TransactionEvent, and MeterValues handlers need OCPP 2.1 registration
- NotifyAllowedEnergyTransfer needs a MessageApi endpoint to trigger sending
- Full end-to-end flow requires extending the above handlers to OCPP_2_VER_LIST

**Section 8: Troubleshooting (per D-09, 3-5 issues)**

1. **Connection drops immediately after WebSocket upgrade**

   - Cause: Wrong subprotocol. Port 8083 requires `ocpp2.1`.
   - Fix: Use `-s ocpp2.1` flag: `wscat -c ws://localhost:8083/CS001 -s ocpp2.1`

2. **Connection refused on port 8083**

   - Cause: CitrineOS container not ready. The container has a 60-second start period.
   - Fix: Run `docker compose ps` and wait until `citrine` shows `healthy`. First start takes longer due to build step.

3. **"Unknown identifier" authentication error**

   - Cause: Missing station ID in URL path.
   - Fix: Include the station ID: `ws://localhost:8083/CS001`, not `ws://localhost:8083/` or `ws://localhost:8083`.

4. **NotifyEVChargingNeeds returns "Rejected"**

   - Cause: No active transaction exists on the EVSE. The handler queries for a transaction record.
   - Fix: This is expected when TransactionEvent returns NotSupported (no transaction created). To test the BPT profile calculation, create a transaction via the REST API (POST to `/data/transaction`) or connect using `ocpp2.0.1` subprotocol on port 8081 for the TransactionEvent step.

5. **"NotSupported" CallError for Authorize/TransactionEvent/MeterValues**
   - Cause: These handlers are only registered for OCPP 2.0.1, not 2.1.
   - Fix: This is the current state of the implementation. These handlers need to be extended with `OCPP_2_VER_LIST` registration. See the "Summary of V2X Capabilities Implemented" section for the complete status.

**Section 9: OCPP Message Framing Reference**

Brief reference:

- Call (station -> CSMS): `[2, "uniqueId", "Action", {payload}]`
- CallResult (CSMS -> station): `[3, "uniqueId", {payload}]`
- CallError (CSMS -> station): `[4, "uniqueId", "errorCode", "description", {details}]`

Where MessageTypeId: Call=2, CallResult=3, CallError=4.
</action>
<verify>
<automated>test -f docs/v2x-discharge-guide.md && grep -c "ws://localhost:8083" docs/v2x-discharge-guide.md && grep -c "NotSupported" docs/v2x-discharge-guide.md && grep -c "DC_BPT" docs/v2x-discharge-guide.md && grep -c "wscat" docs/v2x-discharge-guide.md && grep -c "docker compose" docs/v2x-discharge-guide.md && wc -l docs/v2x-discharge-guide.md</automated>
</verify>
<acceptance_criteria> - docs/v2x-discharge-guide.md exists and is at least 300 lines - File contains "ws://localhost:8083/CS001" (WebSocket URL with station ID) - File contains "-s ocpp2.1" (subprotocol flag for wscat) - File contains "docker compose up" (Docker setup command) - File contains "BootNotification" with JSON example including "V2X-DC-50" model - File contains "StatusNotification" with JSON example - File contains "Authorize" with NotSupported CallError response - File contains "TransactionEvent" with all three eventTypes: "Started", "Updated", "Ended" - File contains "NotifyEVChargingNeeds" with DC_BPT and v2xChargingParameters including "maxDischargePower":50000 - File contains "NotifyAllowedEnergyTransfer" with CSMS-to-station direction documented - File contains "MeterValues" with "Energy.Active.Export.Register" measurand - File contains "Discharging" chargingState value - File contains a "Troubleshooting" section with at least 3 numbered items - File contains handler support matrix table with "Yes" and "No" entries - File contains "SPDX-License-Identifier" header - File does NOT contain instructions for local (non-Docker) development setup
</acceptance_criteria>
<done>
A standalone markdown guide exists at docs/v2x-discharge-guide.md that documents the complete V2X DC discharge flow with full JSON examples for all 9 messages (per D-04), wscat commands (per D-08), Docker-only setup (per D-02), handler support matrix showing 3 NotSupported gaps, and a troubleshooting section with 5 common issues (per D-09). All JSON examples use the commercial ~50kW DC_BPT scenario (per D-06) with station ID CS001 (per D-07).
</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify guide accuracy and completeness</name>
  <what-built>
    V2X discharge connection guide at docs/v2x-discharge-guide.md covering Docker setup, WebSocket connection, 9-message V2X flow with JSON examples, handler support matrix, and troubleshooting.
  </what-built>
  <how-to-verify>
    1. Open docs/v2x-discharge-guide.md and review the overall structure
    2. Verify the handler support matrix matches your understanding of the codebase
    3. Check that all 9 messages from D-04 are present: BootNotification, StatusNotification, Authorize, TransactionEvent (Started), NotifyEVChargingNeeds, NotifyAllowedEnergyTransfer, MeterValues, TransactionEvent (Updated), TransactionEvent (Ended)
    4. Verify JSON examples are valid and use the DC_BPT scenario (~50kW)
    5. Confirm wscat commands are copy-paste ready
    6. Check that NotSupported responses are correctly documented for Authorize, TransactionEvent, and MeterValues
    7. Review troubleshooting section for accuracy
    8. (Optional) Start CitrineOS with `cd Server && docker compose up -d`, wait for healthy, then test Steps 1-2 with wscat: `wscat -c ws://localhost:8083/CS001 -s ocpp2.1`
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- docs/v2x-discharge-guide.md exists with 300+ lines
- Guide covers all 9 messages from the V2X discharge flow (D-04)
- Each message has both station request AND expected CSMS response JSON (D-03, D-05)
- wscat commands included for each step (D-08)
- Docker-only setup instructions (D-02)
- Troubleshooting section with 3-5 items (D-09)
- Handler support matrix honestly documents 3 NotSupported gaps
- DC_BPT scenario with ~50kW values (D-06)
- Station ID CS001 (D-07)
</verification>

<success_criteria>

1. docs/v2x-discharge-guide.md exists and contains the complete V2X discharge flow documentation
2. A developer reading the guide understands which messages work (BootNotification, StatusNotification, NotifyEVChargingNeeds) and which return NotSupported (Authorize, TransactionEvent, MeterValues)
3. All JSON examples are copy-paste ready with valid OCPP 2.1 message framing
4. The guide can be followed from zero (clone, docker compose up, wscat connect, send messages) without external documentation
5. Human verification confirms accuracy
   </success_criteria>

<output>
After completion, create `.planning/phases/03-integration-and-connection-guide/03-01-SUMMARY.md`
</output>
